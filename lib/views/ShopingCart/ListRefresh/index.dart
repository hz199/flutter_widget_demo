import 'package:flutter/material.dart';

class ListRefreshPage extends StatelessWidget {
  const ListRefreshPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('11'),
    );
  }
}
  